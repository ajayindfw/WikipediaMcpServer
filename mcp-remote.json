{
  "mcpServers": {
    "wikipedia-remote": {
      "command": "node",
      "args": [
        "mcp-http-bridge.js"
      ],
      "description": "Wikipedia MCP Server deployed on Render - provides search, sections, and content retrieval via HTTP bridge",
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}