{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Using .NET SDK version from global.json:' && dotnet --version && dotnet restore src/WikipediaMcpServer/WikipediaMcpServer.csproj && dotnet publish src/WikipediaMcpServer/WikipediaMcpServer.csproj -c Release -o ./publish --no-restore"
  },
  "deploy": {
    "startCommand": "dotnet ./publish/WikipediaMcpServer.dll",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "environments": {
    "production": {
      "deploy": {
        "startCommand": "dotnet ./publish/WikipediaMcpServer.dll"
      }
    }
  }
}